# 红区资料 —— 绝不进 git（详见 PROJECT_BRIEF / ENVIRONMENT.local.md）
ENVIRONMENT.local.md
.redzone_patterns.local
PROJECT_BRIEF.md

# golden fixture 从真实生产命令抽出 -> 含站点坐标，只留本机
tests/fixtures/*.local.*
ewave_donau_kit/
ewave_donau_kit.tar.gz
references/ewave_donau_kit/
references/probes/
*_probe_*.txt
gdsout_setup*
# 下面这份是纯占位符模板，不是真的 setup 文件（git 不支持行尾注释，注释必须单独一行）
!mvp/redzone/gdsout_setup.tmpl
# 合成的解析器 fixture：形状照真实文件，值全是明显的假占位符（见该目录的 README.md）。
# discover.py 的测试没有它就只能拿红区文件当输入，而红区文件永不进 git。
!tests/fixtures/offdir_synthetic/gdsout_setup

# 站点坐标（真实内网路径）—— 模板 site.example.sh 进 git，实际值不进
site.local.sh

# 钉在这台机器上的站点坐标（ptxt 版本目录 / gdsout 模板 / 默认表）—— 方案 A，
# 用户点 Tools -> Adopt site coordinates 才写。里面是站点身份（硬约束 1b），
# 且**按定义没有端口表**（见 core/sitepin.py 的红线）。deploy.sh 的 PRESERVE 保着它。
# `*.local.*` 那条通配其实已经盖住它了，这里显式再写一条是给读 .gitignore 的人看的。
site_facts.local.json

# 界面自动存的「上次那份设定」（library / cell / view / 官方 run 目录路径）——
# 全是站点标识符（硬约束 1b）。只存设定、不存解析出来的坐标，理由见
# gui.state.GuiState.save_session。deploy.sh 的 PRESERVE 保着它不被升级吃掉。
session.local.json

# 构建产物
mvp/mvp_pack.sh
mvp/dist/
deploy/dist/
# 部署运行时状态（备份 / staging / scratch）——只存在于装好的那台机器上。
# 开发机上跑一次 deploy/doctor.sh 也会建出来，别让它混进提交。
.deploy/

# 本机专属（写死了 memory 目录的绝对路径 → 换机器需重建，见记忆 memory-maintenance-discipline）
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# 运行产物
*.s[0-9]p
*.s[0-9][0-9]p
*.s[0-9][0-9][0-9]p
*.msh
ewave.log
batches/

# 合成 fixture（P4 logparse）——「运行产物」那几条会误伤它，这里放行。
# 它们不是运行产物，是手写的测试素材：行格式照抄证据、**数值全是编的**
# （出处表见 tests/fixtures/ewave_log_synthetic/README.md）。
# 不放行的话上面的 `ewave.log` 和 `*.s[0-9]p` 会把它们挡在 git 外面 ——
# 本机测试全绿，别人克隆下来却少文件，而且是**静默**少。
!tests/fixtures/ewave_log_synthetic/**
# 但真实日志永远挡着：这两条把 `*.local` / `*.local.*` 重新排除回去，
# 免得有人图省事把红区日志放进这个已放行的目录。
# （真实日志的正确落点是 tests/fixtures/*.local.* —— 见第 7 行那条规则，
#   目录形式请用 `<name>.local.d/`，它同样命中那条。）
tests/fixtures/ewave_log_synthetic/**/*.local
tests/fixtures/ewave_log_synthetic/**/*.local.*
